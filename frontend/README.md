# Foundry Local RAG Assistant — Web Frontend

React + Vite + TypeScript client for the FastAPI backend in `../api.py`. See the
root [README.md](../README.md) for setup and run instructions (`npm install`,
`npm run dev`, and starting the backend separately).
